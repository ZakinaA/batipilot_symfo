<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>

    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
</head>
<body>
    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            background: #f5f7fb;
            font-family: Inter, sans-serif;
        }

        .layout {
            display: flex;
            min-height: 100vh;
        }

        .sidebar {
            position: fixed;          /* clé */
            top: 0;
            left: 0;
            width: 240px;
            height: 100vh;            /* clé */
            background: #ffffff;
            box-shadow: 2px 0 10px rgba(0,0,0,0.05);
            padding: 20px 15px;
            box-sizing: border-box;
        }

            .content {
                margin-left: 240px;       
                flex: 1;
                padding: 30px;
            }
            .menu {
            list-style: none;
            padding: 0;
            margin: 20px 0;
        }

        .menu-item {
            font-weight: 600;
            cursor: pointer;
            margin-bottom: 10px;
        }

        .submenu {
            list-style: none;
            padding-left: 15px;
            margin-top: 5px;
            display: none;
        }

        .submenu li {
            margin: 5px 0;
        }

        .submenu a {
            text-decoration: none;
            color: #555;
            font-size: 14px;
        }

        .menu-item:hover .submenu {
            display: block;
        }
    </style>

    <div class="layout">
        <aside class="sidebar">
            <div class="nomSite">
                <h1>Batipilot</h1>
            </div>

            <ul class="menu">
                <li class="menu-item">
                    Chantiers
                    <ul class="submenu">
                        <li><a href="{{ path('app_chantier_index') }}">Liste les chantiers</a></li>
                        <li><a href="{{ path('app_chantier_new') }}">Ajouter un chantier</a></li>
                    </ul>
                </li>
                <br>
                <li class="menu-item">
                    Postes
                    <ul class="submenu">
                        <li><a href="{{ path('app_poste_index') }}">Liste les postes</a></li>
                        <li><a href="{{ path('app_poste_new') }}">Ajouter un poste</a></li>
                    </ul>
                </li>
                <br>
                <li class="menu-item">
                    Étapes
                    <ul class="submenu">
                        <li><a href="{{ path('app_etape_index') }}">Liste des étapes</a></li>
                        <li><a href="{{ path('app_etape_new') }}">Ajouter une étape</a></li>
                    </ul>
                </li>
            </ul>
        </aside>


        <main class="content">
            {% block body %}{% endblock %}
        </main>
    </div>
</body>
</html>
